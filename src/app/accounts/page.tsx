import AccountsApp from '@/components/accounts/AccountsApp'

export const metadata = { title: 'Account Strategy & Planning — V.Two Ops' }

export default function AccountsPage() {
  return <AccountsApp />
}
